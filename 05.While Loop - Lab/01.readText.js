function readText(input) {
  let index = 0; // тръгваме от нулевия индекс
  let word = input[index]; // взимаме първото име от масива
  while (word !== "Stop") {
    console.log(word);
    index++;
    word = input[index];
    //index++; // индексът се увеличава с 1 и от 0 става 1
    //if (word === "Stop") {
    //  break;
  }
    //index++; // индексът се увеличава с 1 и от 0 става 1
    
     // индексът се увеличава с 1 и от 0 става 1
  }


readText([
  "Nakov",
  "SoftUni",
  "Sofia",
  "Bulgaria",
  "SomeText",
  "Stop",
  "AfterStop",
  "Europe",
  "HelloWorld",
]);

function solve() {
    let name = "Lily";
    let age = 35
    console.log (name + " " + age );
    console.log (`${name} ${age}`); // placeholder под Ескейп, 
}                                   // няма нужда от плюс и празно място
                                    // подпъхваме динамично ПРОМЕНЛИВИТЕ!
solve();